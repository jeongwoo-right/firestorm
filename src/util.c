#include "../include/util.h"

// 수식 계산에 활용하기 위한 util
// 추가되는 연산이 생길 수도 있으니, 파일 분리

int max(int a, int b) { return a > b ? a : b; } // game.c에서 1회 사용
int min(int a, int b) { return a < b ? a : b; } // 현재 안쓰임
